# Vite Configuration

File: `vite.config.js`

## Purpose

Configures the Vite development and build pipeline.

## What it does

- imports `defineConfig` from Vite
- enables the React SWC plugin
- keeps the config minimal and focused on React support

## Notes

The app runs as a standard Vite React project with no custom aliases or server overrides in this file.
