# Input

File: `src/components/Input.jsx`

## Purpose

A reusable text input with the app's shared glass-style appearance.

## Props

- `type`: input type, defaults to `text`
- `placeholder`
- `value`
- `onChange`

## Behavior

- renders a standard HTML `<input>`
- applies the same visual treatment used across the login and admin forms
