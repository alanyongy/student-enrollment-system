# Tailwind Configuration

File: `tailwind.config.js`

## Purpose

Tells Tailwind which files to scan for utility classes.

## Content paths

- `./index.html`
- `./src/**/*.{js,jsx,ts,tsx}`

## Notes

The theme is left mostly unextended, so the app relies on Tailwind defaults plus page-level utility classes.
